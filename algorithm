begin
  t = 0
  initialize P(t)
  evaluate P(t)
  while (!termination_condition)
    t = t + 1
    select_parents(P(t-1))
    select_dead(P(t-1))
    crossover(selected, P(t))
    evaluate(P(t))
  end
end
